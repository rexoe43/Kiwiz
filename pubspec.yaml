name: kiwiz
description: Kiwiz - Asistente de Estudio Inteligente
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  
  flutter:
    sdk: flutter
  
  device_info_plus: ^9.0.0

  package_info_plus: ^4.0.0
  
  cupertino_icons: ^1.0.6
  
  supabase_flutter: ^2.0.0
  
  provider: ^6.0.0
  
  flutter_dotenv: ^5.1.0
  
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
  
  